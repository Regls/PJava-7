
List<Venda> vendas = new ArrayList<>(); // endereço de memoria diferente

        Cliente cliente = new Cliente("Renan", 1202033, 10, 1);
        List<Venda> vendas2 = cliente.getVendas(); // usa o mesmo endreço de memoria
        Venda venda = new Venda();
        cliente.getVendas().add(venda);
        vendas2.add(venda);
        vendas.add(venda);
        cliente.getVendas().remove(venda);
        cliente.getVendas().remove(0);

        for (Venda v:cliente.getVendas()){

        }
        for (int i = 0;i<cliente.getVendas().size();i++){

        }